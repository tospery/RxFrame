//
//  Metric.swift
//  Pods
//
//  Created by 杨建祥 on 2020/2/5.
//

import UIKit
import QMUIKit

final public class Metric {
    
//    static var statusBarHeight: CGFloat {
//        return (UIApplication.shared.isStatusBarHidden ? 0 : UIApplication.shared.statusBarFrame.size.height)
//    }
//    
//    static var statusBarHeightConstant: CGFloat {
//        return (UIApplication.shared.isStatusBarHidden ? (isPad() ? (isNotchedScreen() ? 24 : 20) : alternate(notched: (isLandscape() ? 0 : 44), other: 20)) : UIApplication.shared.statusBarFrame.size.height)
//    }
//    
//    static var navigationBarHeight: CGFloat {
//        return (isPad() ? (iosVersion() >= 12.0 ? 50 : 44) : (isLandscape() ? alternate(regular: 44, compact: 32) : 44))
//    }
//    
//    static var navigationContentTop: CGFloat {
//        return (self.statusBarHeight + self.navigationBarHeight)
//    }
//    
//    static var navigationContentTopConstant: CGFloat {
//        return (self.statusBarHeightConstant + self.navigationBarHeight)
//    }
//    
//    static var tabBarHeight: CGFloat {
//        return (isPad() ? (isNotchedScreen() ? 65 : (iosVersion() >= 12.0 ? 50 : 49)) : (isLandscape() ? alternate(regular: 49, compact: 32) : 49) + self.safeBottom)
//    }
//    
//    static var toolBarHeight: CGFloat {
//        return (isPad() ? (isNotchedScreen() ? 70 : (iosVersion() >= 12.0 ? 50 : 44)) : (isLandscape() ? alternate(regular: 44, compact: 32) : 44) + self.safeBottom)
//    }
//    
//    static var safeBottom: CGFloat {
//        return QMUIHelper.safeAreaInsetsForDeviceWithNotch().bottom
//    }
    
}
